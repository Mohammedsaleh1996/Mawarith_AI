إصلاح v14 - UI Boot + Mobile Login

تم إصلاح سبب توقف الواجهة على "جار الفحص" وتعطل الأزرار.
السبب كان خطأ JavaScript في static/app.js عند join لسطر جديد داخل زر تشغيل ngrok.

الإصلاحات:
1) إصلاح SyntaxError في static/app.js حتى تعمل كل أزرار الداشبورد.
2) تعديل صفحة تسجيل الدخول لتعمل بثبات عبر الموبايل/ngrok: بعد نجاح login يتم التحويل مباشرة إلى / بدل انتظار فحص /api/me الذي كان يعلق في بعض متصفحات الموبايل.
3) تم فحص app.js بـ node --check.
4) تم فحص ملفات Python الأساسية بـ py_compile.

طريقة التشغيل:
- اقفل النسخة القديمة CTRL+C.
- فك ضغط هذه النسخة.
- شغل run_dashboard_auto_ngrok.bat.
- افتح محليًا: http://127.0.0.1:8088
- ومن الموبايل عبر رابط ngrok الأساسي فقط، بدون /webhook/wapilot.
